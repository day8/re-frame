;; This file only exists to require the namespaces that we want planck to compile into cache files.
(ns klipse.generate-lib-cljs-cache
  (:require
    [klipse-clj.repl]
    [klipse-clj.macros]
    [react]
    [reagent.core]
    [re-frame.core]))
